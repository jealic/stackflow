<% if current_user %>
  <%= link_to logger_question_path(question),class:"btn btn-info",style:"margin-top:8px;" do%>
    Answer( <%= question.answers.count %> )
  <% end %>
<% else %>
  <%= link_to question_path(question),class:"btn btn-info",style:"margin-top:8px;" do%>
    Answer( <%= question.answers.count %> )
  <% end %>
<% end %> 